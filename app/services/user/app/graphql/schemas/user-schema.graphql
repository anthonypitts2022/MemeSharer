type Query {
  user(id: String!): User
  searchIfUserExist(username: String!): User
  users: [User]
  googleLogin(googleEmail: String!): User
}

type Mutation {
  createOrUpdateUser(input: CreateUserInput): User
}

#===============================================================================
# Users
#===============================================================================

type User {
  id: String
  name: String
  email: String
  profileUrl: String
  errors: Error
  success: Success
  bulkAddReport: [BulkAddReport]
}

#--------------------------------------
# Inputs
#--------------------------------------
input CreateUserInput {
  id: String!
  name: String!
  email: String!
  profileUrl: String!
}

#===============================================================================
# Messages
#===============================================================================

type Error {
  msg: String
}

type Success {
  msg: String
}

type BulkAddReport {
  msg: String
  val: String
}
